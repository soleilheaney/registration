import '@testing-library/jest-dom/vitest';

// Add any global test setup here
// Example: Mock global fetch
// global.fetch = vi.fn();

// Example: Reset all mocks after each test
afterEach(() => {
  vi.clearAllMocks();
});